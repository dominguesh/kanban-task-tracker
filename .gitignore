# Secrets — use 1Password inject / CI vars; never commit real values
.env
.env.*
!.env.example

# Dev-workstation SSH (public keys only; keep local file out of VCS)
.dev/authorized_keys

# Dependencies & build output
**/node_modules/
**/dist/
**/build/

# Logs & OS noise
*.log
.DS_Store

# Python
**/__pycache__/
*.pyc
