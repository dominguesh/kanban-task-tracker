# Copy to `.env` and inject secrets locally (e.g. op inject). Do not commit `.env`.
# Compose reads env automatically if present; dev defaults remain in docker-compose.dev.yml.

# DATABASE_URL=postgresql://kanban_app:kanban_app@db:5432/kanban_app
