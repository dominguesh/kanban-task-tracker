# Dependencies & build outputs (smaller Docker build context)
**/node_modules
**/dist
**/build
**/.next
**/coverage

# Version control & IDE
.git
.cursor

# Local SSH keys for dev-workstation (never send to build daemon)
.dev/authorized_keys

# Logs & env files (never bake secrets into images)
**/*.log
**/.env
**/.env.*
